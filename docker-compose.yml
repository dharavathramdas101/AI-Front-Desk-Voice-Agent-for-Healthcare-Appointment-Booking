version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./ai_front_desk.db:/app/ai_front_desk.db
    restart: unless-stopped
